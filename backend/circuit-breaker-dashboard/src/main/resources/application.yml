server:
  port: 8092

spring:
  application:
    name: CIRCUIT-BREAKER-DASHBOARD

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"

turbine:
  aggregator:
    clusterConfig: USER-MEETING
  appConfig: user-meeting
