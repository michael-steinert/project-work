eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}

spring:
  cloud:
    config:
      #enabled: true
      #uri: http://localhost:8888
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER
      failFast: true