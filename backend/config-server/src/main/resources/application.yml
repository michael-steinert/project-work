spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: C:/Users/stein/Desktop/project-work/backend/config-repository
          clone-on-start: true

server:
  port: 8093

