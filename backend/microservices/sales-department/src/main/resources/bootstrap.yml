eureka:
  client:
    fetchRegistry: true
  instance:
    preferIpAddress: true
spring:
  application:
    name: sales-department-service
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t